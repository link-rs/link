[target.xtensa-esp32s3-none-elf]
runner = "probe-rs run --chip=esp32s3 --probe=0403:6010 --preverify --always-print-stacktrace --no-location --catch-hardfault"

[env]
DEFMT_LOG="debug"

[build]
rustflags = [
  "-C", "link-arg=-nostartfiles",
  "-Z", "stack-protector=all",
]

target = "xtensa-esp32s3-none-elf"

[unstable]
build-std = ["alloc", "core"]
